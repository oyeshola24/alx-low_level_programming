#include <stdio.h>
/**
   * main - Prints command on to the screen
   *
   * Return the value zero
   */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
